/**
 * Interface representing a game component.
 * Components are modular building blocks that can be added to entities in the game.
 * They encapsulate specific features or behaviors.
 * @interface
 */
export interface Component {}
