import { Component } from './component';

/**
 * Represents a component indicating that an entity should be removed from the game.
 * @implements {Component}
 */
export class RemoveEntityComponent implements Component {}
