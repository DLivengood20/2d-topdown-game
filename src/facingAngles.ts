export enum FacingAngles {
  Top = Math.PI,
  Bottom = 0,
  Left = (90 * Math.PI) / 180,
  Right = (-90 * Math.PI) / 180,
  TopLeft = (135 * Math.PI) / 180,
  TopRight = (-135 * Math.PI) / 180,
  BottomLeft = (45 * Math.PI) / 180,
  BottomRight = (-45 * Math.PI) / 180,
}
